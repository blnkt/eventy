<div class="container">
  <div class="row">
    <h3><%= @user.name if @user.name %></h3>
    <div class="col-md-2 profile">
      <img src=<%= @user.image %> alt="Profile Pic">
    </div>
  </div>
</div>
